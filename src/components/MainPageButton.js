function MainPageButton(props) {
    return (
        <div>
            <button>
                <link>{props.message}</link>
            </button>
        </div>
    )
}

export default MainPageButton